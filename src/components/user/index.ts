// Local overrides of astro-pure/user components. Kept as a barrel so call sites
// read like the upstream ones (`import { Button } from 'astro-pure/user'`).
export { default as Button } from './Button.astro'
