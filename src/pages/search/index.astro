---
import { PFSearch } from 'astro-pure/components/pages'
import PageLayout from '@/layouts/BaseLayout.astro'
import { Button } from '@/components/user'
import { integ } from '@/site-config'

const meta = {
  description: 'Search all posts on this site',
  title: 'Search'
}
---

<PageLayout {meta}>
  <Button title='Back' href='/blog' variant='back' />
  <main class='mt-6 lg:mt-10'>
    <div id='content-header' class='animate'>
      <h1 class='mb-6 text-3xl font-medium'>Search</h1>
    </div>

    <div id='content' class='animate'>
      {
        integ.pagefind ? (
          <>
            <p>Type a keyword to search posts on this site.</p>
            <PFSearch />
          </>
        ) : (
          <p>Pagefind is disabled.</p>
        )
      }
    </div>
  </main>
</PageLayout>
